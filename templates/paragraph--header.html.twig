{#
/**
 * @file
 * HEADER paragraph template.
 *
 * @todo replace current parallax/video libraries with ONE single library,
 *       that supports both images and video!
 */
#}
{% set parallax = parade.enable_parallax %}
{%
set classes = [
  'paragraph',
  'paragraph--type--' ~ paragraph.bundle|clean_class,
  view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
  'paragraph--background-' ~ parade.background.type|clean_class,
]
%}

{# Attach library if Parallax is enabled #}
{% if parallax %}
  {{ attach_library('parade/parallax') }}
{% endif %}
{% if parade.background.type == 'video' %}
  {{ attach_library('parade/iphone-inline-video') }}
{% endif %}

<div{{ attributes.addClass(classes) }}>
  <div class="background">

    {# Header background #}
    {% if parallax %}
    <div class="background-wrapper parallax" data-rellax-speed="-7">
    {% else %}
    <div class="background-wrapper">
    {% endif %}

      {% if parade.background.type == 'image' %}
        <div class="background-image" style="background-image: url({{- parade.background.url -}})"></div>
      {% elseif parade.background.type == 'video' %}
        <video class="background-video" autoplay muted loop webkit-playsinline src="{{ parade.background.url }}" poster="{{ parade.background.poster }}"></video>
      {% endif %}

    </div>

    {# Header content within the container #}
    <div class="paragraph-inner">
      {{ content }}
    </div>

  </div>
</div>
